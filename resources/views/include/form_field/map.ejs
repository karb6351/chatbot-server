<div class="field">
    <label class="label"><%- key.charAt(0).toUpperCase() + key.slice(1);  %></label>
    <div class="control">
      <google-map name="<%= key %>" 
        gps="<%= typeof obj !== 'undefined' && obj ? obj[key] : defaultValue %>"
        ></google-map>
    </div>
  </div>